# OneBhoomi Core Dependencies
cryptography>=41.0.0
argon2-cffi>=23.1.0
reportlab>=4.0.0
qrcode>=7.4.0
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
requests>=2.31.0
pypdf>=3.17.0
pypdfium2>=4.26.0

# Optional: PostgreSQL Database Pooling (Native JSON store is active by default)
psycopg[binary,pool]>=3.1.0

# Optional: PaddleOCR & Local Vision Engine (Can also run via remote Kaggle/Colab GPU worker)
paddleocr>=2.7.0
paddlepaddle>=2.5.0
